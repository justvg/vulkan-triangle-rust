[package]
name = "vulkan_test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
winit = "0.26"
ash = { version = "0.35.1", features = ["linked", "debug"] }
ash-window = "0.9.0"